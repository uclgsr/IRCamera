# Disabled CI/CD workflow - replaced with streamlined approach
# Original super-linter workflow disabled per request
---
name: Disabled Lint Code Base

'on':
  # Disabled - no triggers
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Force run this disabled workflow'
        required: false
        default: false

jobs:
  disabled-lint:
    if: false  # Always disabled
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow has been disabled per request"
