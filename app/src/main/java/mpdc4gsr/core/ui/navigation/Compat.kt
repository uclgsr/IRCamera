package mpdc4gsr.core.ui.navigation

/**
 * Backward compatibility file for navigation package refactoring
 * This allows existing code to continue using the old package name
 * while new code uses the core.ui.navigation package
 */

// Import and re-export for backward compatibility
import mpdc4gsr.core.ui.navigation.UnifiedNavHost
import mpdc4gsr.core.ui.navigation.UnifiedRoute

