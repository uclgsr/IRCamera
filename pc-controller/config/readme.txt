Configuration Files
===================

This directory contains configuration files for legacy implementations.

- config.yaml: Legacy MVP configuration
- protocol.json: Protocol specification

For the main PC controller (pc_controller.py), see ../config.yaml in the root directory.
